# Lightweight requirements for Railway deployment
# Core API dependencies only
elevenlabs==2.16.0
openai==2.2.0
requests==2.32.5
python-dotenv==1.1.1

# Web API dependencies
flask==3.1.2
flask-cors==6.0.1

# Audio processing for full pipeline (optional - will fallback if unavailable)
whisper-timestamped==1.15.9
moviepy==2.2.1

# Video processing dependencies for MoviePy
imageio==2.37.0
imageio-ffmpeg==0.6.0
decorator==5.1.1
proglog==0.1.10

# Lightweight data processing (Python 3.10 compatible)
numpy==1.26.4
pillow==10.4.0

# Async support
aiohttp==3.13.0

# Utilities
tqdm==4.67.1
pydantic==2.12.0
tiktoken==0.12.0

# Cloud Storage
boto3==1.35.0

# Remove heavy dependencies:
# - torch (900MB+)
# - scipy (35MB+)
# - whisper-timestamped (requires torch)
# - CUDA libraries (2GB+)
